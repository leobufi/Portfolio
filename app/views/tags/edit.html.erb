<div class="container">
  <div class="page-padding">
    <div class="page-title">
      <h1>Éditer <%= @tag.name %></h1>
    </div>
    <div class="form">
      <%= simple_form_for(@tag) do |f| %>
        <%= f.input :name %>
        <div class="form-button">
          <button>
            <%= f.submit %>
          </button>
        </div>
      <% end %>
    </div>
  </div>
</div>
