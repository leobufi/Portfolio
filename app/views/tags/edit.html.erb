<div class="hero-dashboard" data-controller="list random-colors day-night-mode">
  <div class="container">
    <div class="page-padding">
      <div class="page-title">
        <h1>Éditer <%= @tag.name %></h1>
      </div>
      <div class="form">
        <%= simple_form_for(@tag) do |f| %>
          <%= f.input :name %>
          <%= f.input :rate, collection: 1..10 %>
          <%= f.input :categories, as: :check_boxes, collection: Tag::CATEGORY, input_html: { multiple: true }%>
          <div class="form-button">
            <button>
              <%= f.submit %>
            </button>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
